export interface GameConfig {
  canvasID: string;
  width: number;
  height: number;
  backgroundColor: string;
  spawnInterval: number;
  poolSize: number;
  maxPoolSize: number;
  debugSprites: boolean;
}
